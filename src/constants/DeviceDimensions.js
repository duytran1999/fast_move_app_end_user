import { Dimensions } from 'react-native'

export const HEIGHT_DEVICE_SCREEN = Dimensions.get('screen').height

export const WIDTH_DEVICE_SCREEN = Dimensions.get('screen').width

export const HEIGHT_DEVICE_WINDOW = Dimensions.get('window').height

export const WIDTH_DEVICE_WINDOW = Dimensions.get('window').width